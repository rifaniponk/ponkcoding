# Netlify build config. `npm run build` runs the content pipeline (prebuild),
# type-checks, and emits static assets to dist/. SPA-fallback routing lives in
# public/_redirects (copied into dist).
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
